import * as React from 'react';

export const Home = () => {
    return (
        <div className={'hello'} style={{ textAlign: 'center' }}>
            <p>Home</p>
        </div>
    );
};

export default Home;
